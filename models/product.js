class Product {
  constructor(id, ownerId, title, imegeUrl, description, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.imegeUrl = imegeUrl;
    this.description = description;
    this.price = price;
  }
}

export default Product;
